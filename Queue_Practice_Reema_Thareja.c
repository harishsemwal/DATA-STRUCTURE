#include<stdio.h>
#include<stdlib.h>

struct arrayQueue
{
    int front, rear;
    int capcity;
    int *array;
};

struct arrayQueue *createQueue()
{
    struct arrayQueue *temp;
    temp = malloc(struct arrayQueue)malloc(sizeof(struct arrayQueue *));
    return temp;
}

int isFull()
{
    if()
}